# Aulas 11-14 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans para implementar este plano task-by-task. Steps usam checkbox (`- [ ]`).

**Goal:** Entregar as 4 aulas finais do módulo DevOps T13 2026.1B (Performance, Terraform local, SRE SLO, IA + Coding) com PRs reais no demo PX4 (#7, #8, #9) e HTMLs Reveal.js no repo de aulas.

**Reference spec:** `docs/superpowers/specs/2026-05-22-aulas-11-14-design.md` (commit `7de8509`)

**Repos:**
- `inteli-2026-1b-t13-es10` (este) — HTMLs das aulas + index + assets
- `inteli-px4-cicd-demo` — PRs #7, #8, #9 com código + ADRs

---

## Visão geral das fases

| Fase | Aula | Deliverables | Prazo (spec) |
|------|------|--------------|--------------|
| **Fase 0** | — | Preparação: numeração ADR, branches, snapshot PRs #5/#6 | 28/05 EOD |
| **Fase 1** | A11 (03/06) | PR #7 demo + `aula11.html` + 4 prints + ADR-008 | 02/06 EOD |
| **Fase 2** | A12 (11/06) | PR #8 demo + `aula12.html` + 4 prints + ADR-009 | 10/06 EOD |
| **Fase 3** | A13 (16/06) | PR #9 demo + `aula13.html` + 4 prints + ADR-010 | 15/06 EOD |
| **Fase 4** | A14 (22/06) | `aula14.html` + `aulas/aula14-material/*` + 3 prints + slide fechamento módulo | 21/06 EOD |

**Detalhe granular:** Fase 0 e Fase 1 completas abaixo. Fases 2-4 em outline — cada uma vira plano detalhado próprio após Fase anterior mergeada (lições do PR #N informam o #N+1).

**Paralelismo:** Fase 1 deve completar antes de iniciar Fases 2 e 3 (que dependem do runner rápido do PR #7). Fase 4 pode ser produzida em paralelo a partir do meio da Fase 3, pois usa apenas material existente.

---

## File Structure (estado final pós Fase 4)

Repo `inteli-px4-cicd-demo` — adiciona:

```
infra/terraform/runner-local/         — Fase 2 (PR #8)
  versions.tf
  main.tf
  variables.tf
  outputs.tf
  cloud-init/runner-setup.yaml
  README.md
slos.yaml                             — Fase 3 (PR #9)
metrics/history.jsonl                 — Fase 3 (PR #9)
tools/error_budget.py                 — Fase 3 (PR #9)
docs/performance.md                   — Fase 1 (PR #7)
docs/sre-basics.md                    — Fase 3 (PR #9)
docs/adrs/
  ADR-NNN-pipeline-perf.md            — Fase 1
  ADR-NNN-terraform-multipass-local.md — Fase 2
  ADR-NNN-multi-window-slo.md         — Fase 3
.github/workflows/
  ci.yml                              — atualizada nas fases 1 e 3
  runner-iac-ci.yml                   — Fase 2 (novo)
  slo-comment.yml                     — Fase 3 (novo)
tests/conftest.py                     — atualizado na Fase 1
tools/run_mission.py                  — atualizado na Fase 1
```

Repo `inteli-2026-1b-t13-es10` — adiciona:

```
aulas/aula11.html                     — Fase 1
aulas/aula12.html                     — Fase 2
aulas/aula13.html                     — Fase 3
aulas/aula14.html                     — Fase 4
aulas/aula14-material/
  wanted-features.md                  — Fase 4
  prompt-pack.md                      — Fase 4
  pr-snapshots/                       — Fase 0 (snapshot inicial) + Fase 4
aulas/assets/img/aula11/              — Fase 1, 4 PNGs
aulas/assets/img/aula12/              — Fase 2, 4 PNGs
aulas/assets/img/aula13/              — Fase 3, 4 PNGs
aulas/assets/img/aula14/              — Fase 4, 3 PNGs
aulas/index.html                      — atualizado ao fim de cada fase
```

---

## Fase 0 — Preparação geral

**Objetivo:** confirmar numeração ADR no demo, criar branches base, snapshot dos PRs #5/#6 antes que possam ser mergeados/fechados.

### Task 0.1: Confirmar último ADR no demo

**Files:** nenhum modificado, leitura apenas.

- [ ] **Step 1:** No `~/Projects/Inteli/.../inteli-px4-cicd-demo/`, rodar:

```bash
ls docs/adrs/ | sort -V | tail -3
```

Expected: lista ADRs por número. Anotar o último número N. Os 3 novos ADRs serão N+1, N+2, N+3.

- [ ] **Step 2:** Atualizar a spec se a numeração diferir de 008/009/010:

```bash
# Em docs/superpowers/specs/2026-05-22-aulas-11-14-design.md
# Substituir ADR-008/009/010 pelos números corretos
```

### Task 0.2: Criar branches base no demo

- [ ] **Step 1:** Criar 3 branches a partir do main mais recente do demo:

```bash
cd ~/Projects/Inteli/.../inteli-px4-cicd-demo
git checkout main && git pull
git checkout -b feat/pr7-pipeline-fast && git push -u origin feat/pr7-pipeline-fast
git checkout main
git checkout -b feat/pr8-terraform-runner && git push -u origin feat/pr8-terraform-runner
git checkout main
git checkout -b feat/pr9-slo-error-budget && git push -u origin feat/pr9-slo-error-budget
git checkout main
```

Expected: 3 branches no remote, sem mudanças ainda.

### Task 0.3: Snapshot dos PRs #5 e #6

**Files:**
- Create: `aulas/aula14-material/pr-snapshots/pr-05-rafael.diff`
- Create: `aulas/aula14-material/pr-snapshots/pr-06-vinicius.diff`
- Create: `aulas/aula14-material/pr-snapshots/pr-05-meta.json`
- Create: `aulas/aula14-material/pr-snapshots/pr-06-meta.json`

- [ ] **Step 1:** Baixar diff e metadados:

```bash
mkdir -p aulas/aula14-material/pr-snapshots
gh pr view 5 --repo josercf/inteli-px4-cicd-demo --json title,body,author,createdAt,files > aulas/aula14-material/pr-snapshots/pr-05-meta.json
gh pr diff 5 --repo josercf/inteli-px4-cicd-demo > aulas/aula14-material/pr-snapshots/pr-05-rafael.diff
gh pr view 6 --repo josercf/inteli-px4-cicd-demo --json title,body,author,createdAt,files > aulas/aula14-material/pr-snapshots/pr-06-meta.json
gh pr diff 6 --repo josercf/inteli-px4-cicd-demo > aulas/aula14-material/pr-snapshots/pr-06-vinicius.diff
```

Expected: 4 arquivos criados, diffs com conteúdo real.

- [ ] **Step 2:** Commit no repo de aulas:

```bash
git add aulas/aula14-material/pr-snapshots/
git commit -m "chore(aula14): snapshot dos PRs #5 e #6 do demo para material da aula"
git push origin main
```

---

## Fase 1 — Aula 11 (Performance)

**Objetivo:** PR #7 mergeado verde no demo, `aula11.html` produzida com 4 prints reais, ADR de perf commitada, index atualizado.

### Task 1.1: Criar `aula11.html` baseado em template `aula10.html`

**Files:**
- Create: `aulas/aula11.html`

- [ ] **Step 1:** Copiar aula10 como template:

```bash
cp aulas/aula10.html aulas/aula11.html
```

- [ ] **Step 2:** Editar cabeçalho, agenda, blocos conforme seção 4 da spec. Estrutura de slides:

1. Capa (Aula 11, Performance, Hermano, 03/06)
2. Agenda (6 blocos da tabela 4.1 da spec)
3. Objetivos (4 cartões: profile pipeline, aplicar 4 camadas de cache, reescrever fixtures, paralelizar mission-test)
4. Hook + recap A10
5-8. Bloco 1 (porque está lento) — 4 slides: heatmap, gargalo SITL, gargalo Docker, gargalo waits
9-13. Bloco 2 (cache CI) — 5 slides: anatomia, pip cache, sonar cache, buildx cache, PX4 submódulo cache
14-17. Bloco 3 (SITL warm) — 4 slides: conftest session-scope, await condition, pytest-xdist, mock hardware
18. Bloco 4 (hands-on)
19. Quiz 2 questões
20. Checklist 5 itens
21. Auto-estudo A12 (Terraform + Multipass)
22. Encerramento

- [ ] **Step 3:** Validar localmente:

```bash
npx serve aulas
# abrir http://localhost:3000/aula11.html
# navegar por todos os slides, verificar sem erros no console
```

### Task 1.2: PR #7 no demo — Cache no CI

**Files (no demo):**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1:** Profile real do mission-test atual no PR #4 mergeado:

```bash
cd ~/Projects/Inteli/.../inteli-px4-cicd-demo
gh run view <last-pr4-run-id> --log > /tmp/pr4-log.txt
# Identificar os 3 maiores consumidores de tempo
```

Anotar gargalos para o ADR.

- [ ] **Step 2:** No branch `feat/pr7-pipeline-fast`, adicionar 4 blocos de cache em `ci.yml`:

```yaml
- uses: actions/cache@v4
  with:
    path: ~/.cache/pip
    key: pip-${{ hashFiles('requirements*.txt') }}
- uses: actions/cache@v4
  with:
    path: ~/.sonar/cache
    key: sonar-${{ hashFiles('sonar-project.properties') }}
# buildx layer cache via setup-buildx-action e GHA cache type
# PX4 submodule: cache do diretório PX4-Autopilot e .git/modules
```

- [ ] **Step 3:** Commit + push parcial:

```bash
git add .github/workflows/ci.yml
git commit -m "feat(ci): adiciona cache em pip, sonar, buildx, PX4 submodule"
git push
```

### Task 1.3: PR #7 — SITL warm (fixtures + paralelo)

**Files (no demo):**
- Modify: `tests/conftest.py`
- Modify: `tools/run_mission.py`
- Modify: `tests/integration/test_mission_*.py`
- Modify: `requirements-dev.txt` (add `pytest-xdist`)

- [ ] **Step 1:** Refatorar `conftest.py`: fixtures `sitl_running` e `drone_connected` com `scope="session"`, teardown limpo (kill processes, remove tmpdir).

- [ ] **Step 2:** Substituir `await asyncio.sleep(N)` por `await condition()` em pelo menos 5 pontos do `run_mission.py`. Cada `sleep` vira poll em estado real (arm status, mission upload complete, takeoff altitude, mission complete, landed).

- [ ] **Step 3:** Adicionar `pytest-xdist` e marcar testes paralelizáveis com `@pytest.mark.parametrize` em vez de cópia.

- [ ] **Step 4:** Atualizar `ci.yml` para rodar `pytest -n auto` na missão.

- [ ] **Step 5:** Commit:

```bash
git add tests/ tools/ requirements-dev.txt .github/workflows/ci.yml
git commit -m "feat(perf): fixtures session-scope + xdist + poll-based waits"
git push
```

### Task 1.4: PR #7 — ADR + perf docs

**Files (no demo):**
- Create: `docs/adrs/ADR-NNN-pipeline-perf.md` (NNN = próximo número)
- Create: `docs/performance.md`

- [ ] **Step 1:** Escrever ADR seguindo estrutura do CLAUDE.md global (Data, Status, Decisores, Contexto, Decisão, Motivações, Riscos+mitigações, Consequências, Relacionadas).

- [ ] **Step 2:** `docs/performance.md` com tabela antes/depois por job. Anexar imagem do flame-graph (gerado com `py-spy record`).

- [ ] **Step 3:** Commit + abrir PR:

```bash
git add docs/
git commit -m "docs(perf): ADR + tabela antes/depois do pipeline"
git push
gh pr create --title "PR #7 — Pipeline rápido: cache no CI + SITL warm (Aula 11)" \
  --body "$(cat <<'EOF'
## Summary
Reduz duração do mission-test de ~8min para ~3min via 4 camadas de cache no CI e SITL warm (fixtures session-scope + xdist + waits poll-based).

## Materializa
Aula 11 do módulo DevOps T13 2026.1B (03/06, Hermano).

## Test plan
- [ ] CI verde com todas as 4 caches "hit" após primeira execução
- [ ] mission-test < 4min no runner self-hosted
- [ ] pytest -n 3 executa 3 missões concorrentes sem flake
- [ ] ADR justifica trade-offs de cache (correctness vs speedup)
EOF
)"
```

### Task 1.5: Esperar CI verde do PR #7 e capturar prints

- [ ] **Step 1:** Aguardar CI verde (run-id no gh run list).

- [ ] **Step 2:** Capturar 4 prints (cmd+shift+4 no Mac) salvos em `aulas/assets/img/aula11/`:
  - `01-before-after.png` — split-screen Actions mostrando duração antes (PR #4) e depois (PR #7)
  - `02-cache-hit.png` — log do CI com "Cache hit ✓" para 4 chaves
  - `03-xdist-parallel.png` — output `pytest -n 3` rodando 3 missões
  - `04-flamegraph.png` — flame-graph do mission-test original com hotspot marcado

### Task 1.6: Finalizar `aula11.html`, atualizar index, commit

- [ ] **Step 1:** Substituir todas as referências "*pending*" pelos 4 prints reais.

- [ ] **Step 2:** No `aulas/index.html`, converter Aula 11 de `placeholder` para link ativo (mesmo padrão usado em aula09/10).

- [ ] **Step 3:** Validar local + commit + push:

```bash
npx serve aulas  # smoke check
git add aulas/aula11.html aulas/assets/img/aula11/ aulas/index.html
git commit -m "feat(aula11): performance no SITL e no CI, com PR #7 verde"
git push origin main
```

### Task 1.7: Mergear PR #7 no demo

- [ ] **Step 1:** Após aula11 estar publicada e revisada, mergear PR #7 no main do demo:

```bash
gh pr merge 7 --repo josercf/inteli-px4-cicd-demo --squash
```

---

## Fase 2 — Aula 12 (Terraform + Multipass) — OUTLINE

**Prerrequisito:** Fase 1 mergeada (runner do PR #7 acelera dev de testes do TF).

### Outline de tasks

- **2.1** Instalar Multipass localmente, validar `multipass launch` funciona no laptop do user
- **2.2** Criar `aula12.html` baseado em template (~22 slides)
- **2.3** PR #8 — adicionar `infra/terraform/runner-local/` (5 arquivos `.tf` + cloud-init)
- **2.4** PR #8 — workflow `runner-iac-ci.yml` com fmt/validate/plan + comentário no PR
- **2.5** PR #8 — README detalhado de Multipass install em Mac/Linux/Win + troubleshooting
- **2.6** PR #8 — ADR-NNN-terraform-multipass-local (escopo local, fallback Docker, quando virar cloud)
- **2.7** Aplicar Terraform localmente, registrar runner, capturar 4 prints (`01-terraform-plan.png` etc)
- **2.8** Substituir pending prints em aula12.html, atualizar index, commit/push
- **2.9** Mergear PR #8 no demo

### Pontos críticos

- Provider `larstobi/multipass` é comunitário; validar última versão estável antes de fixar em `versions.tf`
- Cloud-init pode falhar se o token de runner registrar expirar antes do apply — usar PAT temporário com escopo `repo` + `admin:org/manage_runners`
- Hands-on em sala requer cada aluno ter Multipass instalado; ADR-009 documenta fallback Docker para alunos com restrição

---

## Fase 3 — Aula 13 (SRE: SLO + error budget) — OUTLINE

**Prerrequisito:** Fase 1 mergeada (history.jsonl alimentado pelas missions rápidas).

### Outline de tasks

- **3.1** Definir 3 SLOs concretos com thresholds calibrados nos últimos 10-20 runs do mission-test (success rate first-try ≥ 95%, p95 duration ≤ 240s, rollback ≤ 30s)
- **3.2** Criar `aula13.html` (~22 slides)
- **3.3** PR #9 — `slos.yaml` declarativo com múltiplas janelas (1h/24h/7d/30d)
- **3.4** PR #9 — `tools/error_budget.py` (parser YAML, leitor `history.jsonl`, cálculo burn rate por janela)
- **3.5** PR #9 — `metrics/history.jsonl` inicial com 10-20 runs sintéticos para a aula
- **3.6** PR #9 — workflow `slo-check` no `ci.yml` + comentário automático em PR
- **3.7** PR #9 — `docs/sre-basics.md` (glossário SLI/SLO/SLA/budget, exemplos com PR #1-#4)
- **3.8** PR #9 — ADR-NNN-multi-window-slo (por que múltiplas janelas, alternativa rolling 30d só)
- **3.9** Aguardar CI verde, capturar 4 prints (`01-slos-yaml.png`, `02-slo-burn-comment.png`, `03-history-chart.png`, `04-slo-violation.png` — pode requerer injeção de falha proposital)
- **3.10** Substituir pending em aula13.html, atualizar index, commit/push
- **3.11** Mergear PR #9 no demo

### Pontos críticos

- `history.jsonl` precisa ser populado antes do walkthrough; rodar mission-test ~10x no main pós-merge do PR #7 para alimentar dados reais
- O print `04-slo-violation.png` exige rodar com falha proposital (ex.: degradar timeout); fazer em branch separada e arquivar print

---

## Fase 4 — Aula 14 (IA + Coding skills) — OUTLINE

**Prerrequisito:** snapshots dos PRs #5 e #6 já feitos na Fase 0.

### Outline de tasks

- **4.1** Criar `aula14.html` (~22 slides; foco em walkthroughs visuais + hands-on)
- **4.2** `aulas/aula14-material/wanted-features.md` — 8-10 features candidatas (ex.: `compute_thrust`, `battery_health`, `geofence_check`) com critério de aceitação
- **4.3** `aulas/aula14-material/prompt-pack.md` — prompts de partida para 3 níveis (Copilot/Cursor/Claude)
- **4.4** Atualizar `aulas/aula14-material/pr-snapshots/` com anotações (vermelho = problema, verde = bom) renderizadas como imagens
- **4.5** Capturar prints: tabela 4-ferramentas, prompt 3-saídas, codex-PR anotado
- **4.6** Slide final fechando módulo: linha do tempo A01→A14, checklist 14 itens, gancho para próximo módulo
- **4.7** Atualizar index, commit/push (todas as 14 aulas ativas)

### Pontos críticos

- Print `04-student-pr-during-class.png` é capturado AO VIVO; aula14.html referencia placeholder até a aula
- Material `aula14-material/` precisa sobreviver mesmo se PRs #5/#6 forem fechados/mergeados; daí os snapshots na Fase 0

---

## Resumo de commits esperados no `inteli-2026-1b-t13-es10`

| Fase | Commit message (template) |
|------|---------------------------|
| Fase 0 | `chore(aula14): snapshot dos PRs #5 e #6 do demo para material da aula` |
| Fase 1 | `feat(aula11): performance no SITL e no CI, com PR #7 verde` |
| Fase 2 | `feat(aula12): Terraform + Multipass para runner local, com PR #8 verde` |
| Fase 3 | `feat(aula13): SLO + error budget sobre métricas, com PR #9 verde` |
| Fase 4 | `feat(aula14): IA como copiloto crítico + fechamento do módulo` |
| Fase 4 | `feat(index): habilita links das aulas 11-14 (todas com HTML real)` |

---

## Riscos consolidados e mitigação

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Speedup do PR #7 < 50% | Média | Médio | ADR documenta limites; aula refoca em profiling como processo |
| Multipass falha em laptop de aluno | Média | Médio | Fallback Docker documentado em ADR; hands-on em "plan only" como B |
| PRs #5/#6 mergeados/fechados antes A14 | Média | Baixo | Snapshots feitos na Fase 0 com diff + meta |
| Cronograma 1-semana fica apertado | Alta | Médio | Fase 1 detalhada; Fases 2-4 só vira detalhe após anterior fechar; user pode pausar |
| Custos de runtime do hands-on (Multipass + SITL no laptop dos alunos) | Baixa | Baixo | Hands-on opcional; demo guiada por instrutor cobre 80% |

---

## Definição de pronto (replicada da spec, condensada)

Cada fase fecha quando:

1. PR no demo (onde aplicável) está mergeado em main, com CI verde
2. ADR correspondente commitada em `docs/adrs/` do demo
3. HTML da aula no repo de aulas, com 4 prints reais (não pending)
4. Link da aula no `aulas/index.html` é `<a class="aula-card">`, não `<div class="placeholder">`
5. Commit no repo de aulas seguindo Conventional Commits + Co-Authored-By: Claude
